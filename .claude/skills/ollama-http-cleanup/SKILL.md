---
name: ollama-http-cleanup
description: MERGED — use `openai-compatible-cleanup` instead. Ollama is now one preset of the universal OpenAI-compatible engine (v0.1 task V01-2).
---

# Ollama HTTP Cleanup — MERGED

This skill has been superseded by **`openai-compatible-cleanup`**.

Ollama (`http://127.0.0.1:11434/v1`) is the `.ollama` preset of `OpenAICompatibleCleaner`.
The loopback-only constraint, availability check, model table, and error HUDs are all documented there.

**Use `openai-compatible-cleanup` for all V01-2 implementation work.**
